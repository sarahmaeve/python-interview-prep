# Evidence Packet 2

The following sequence was reconstructed for one affected submission:

1. The caller submitted `build-report` as logical operation `operation-7`.
2. The gateway accepted a request and created a job.
3. The response was lost before the caller received it.
4. The Python component made another gateway request.
5. The gateway created another job.
6. The caller received the second response.

There was one caller invocation of `JobSubmitter.submit`; the second gateway
request occurred inside that invocation.

