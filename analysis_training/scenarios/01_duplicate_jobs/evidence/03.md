# Evidence Packet 3

Correlated client and gateway records for one affected submission:

```text
client operation_id=operation-7 payload=build-report attempt=1
gateway idempotency_key=attempt-a accepted job=100
client transient_error="response lost after acceptance"

client operation_id=operation-7 payload=build-report attempt=2
gateway idempotency_key=attempt-b accepted job=101
client returned job=101
```

The operation ID comes from the caller. The idempotency keys shown above are
the values received by the gateway.

