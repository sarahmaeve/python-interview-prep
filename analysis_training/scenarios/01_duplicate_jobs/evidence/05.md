# Evidence Packet 5

The incident increased transient response failures, causing the existing retry
path to run much more often than usual.

The job-submission component was not changed in the deployment preceding the
incident. The recent gateway rollout changed transport behavior but did not
change its repeated-key contract. The problem reproduces with the supported
local Python version and does not depend on operating system or working
directory.

