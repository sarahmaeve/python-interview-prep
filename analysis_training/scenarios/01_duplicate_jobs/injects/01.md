# Follow-Up Inject 1: The Empty Batch

The original duplicate-job reproduction now passes. During verification,
another team reports a narrower symptom:

> The gateway uses job ID `0` for a valid completed empty batch. Calling the
> submitter again with the same operation ID and payload reaches the gateway a
> second time. Completed jobs with positive IDs are returned locally without a
> second gateway call.

Establish whether `0` is a valid completed result—it is—then explain why the
component treats it differently from positive job IDs.

Focused verification:

```bash
cd exercises/40_retry_idempotency
python3 -m unittest \
  test_job_submitter.TestCompletedResultCache.test_falsy_completed_result_is_still_cached \
  -v
```

Do not open Inject 2 until this test passes.

