def mean(numbers):
    """Return the arithmetic mean of a list of numbers.

    Raises ValueError if the list is empty.
    """
    pass


def median(numbers):
    """Return the median of a list of numbers.

    For even-length lists, return the average of the two middle values.
    Raises ValueError if the list is empty.
    The original list must not be modified.
    """
    pass


def mode(numbers):
    """Return the most frequently occurring value.

    If there is a tie, return the smallest value among the tied modes.
    Raises ValueError if the list is empty.
    """
    pass


def std_dev(numbers):
    """Return the population standard deviation.

    Formula: sqrt(mean of squared deviations from the mean).
    Raises ValueError if the list is empty.
    """
    pass


def percentile(numbers, p):
    """Return the p-th percentile (0-100) using linear interpolation.

    Raises ValueError if the list is empty or p is not in [0, 100].
    """
    pass
